import { tv } from "tailwind-variants";

export const title = tv({
  base: "tracking-tight inline font-semibold text-[2.3rem] lg:text-5xl",
});

export const subtitle = tv({
  base: "w-full my-2 text-lg lg:text-xl text-default-600 block max-w-full",
});
